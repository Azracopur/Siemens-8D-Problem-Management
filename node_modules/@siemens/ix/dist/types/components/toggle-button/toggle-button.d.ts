import { EventEmitter } from '../../stencil-public-runtime';
import { BaseButtonVariant } from '../button/base-button.types';
import { ButtonVariant } from '../button/button';
export type ToggleButtonVariant = Exclude<ButtonVariant, `danger-${BaseButtonVariant}`>;
export declare class ToggleButton {
    /**
     * Button variant.
     */
    variant: ToggleButtonVariant;
    /**
     * Disable the button
     */
    disabled: boolean;
    /**
     * Loading button
     */
    loading: boolean;
    /**
     * Icon name
     */
    icon?: string;
    /**
     * Icon name for the right side of the button
     * @since 4.0.0
     */
    iconRight?: string;
    /**
     * Show button as pressed
     */
    pressed: boolean;
    /**
     * ARIA label for the button
     * Will be set as aria-label on the nested HTML button element
     *
     * @since 3.2.0
     */
    ariaLabelButton?: string;
    /**
     * Pressed change event
     */
    pressedChange: EventEmitter<boolean>;
    private dispatchPressedChange;
    render(): any;
}
